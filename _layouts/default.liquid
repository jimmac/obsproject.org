<!DOCTYPE html>
<html>
    {% include head.html %}
    <body>
      {% if page.data.splash %}
        {% include splash.html %}
        {% include header.html %}
      {% else %}
        {% include header.html %}
      {% endif%}

      <div class="container">
        {{ page.content }}
      </div>
      {% include footer.html %}
    </body>
</html>
